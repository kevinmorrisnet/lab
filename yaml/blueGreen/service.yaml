apiVersion: v1
kind: Service
metadata:
  labels:
    app: myapp
  name: blue-green-service
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: myapp
    version: blue
  type: ClusterIP
