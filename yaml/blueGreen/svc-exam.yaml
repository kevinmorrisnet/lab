apiVersion: v1
kind: Service
metadata:
  labels:
    app: webapp
  name: webapp-svc
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: webapp
  type: ClusterIP
status:
  loadBalancer: {}
