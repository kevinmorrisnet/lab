apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-database-ingress
  namespace: netpol-test
spec:
  podSelector:
    matchLabels:
      app: database
  policyTypes:
  - Ingress
  # No ingress rules = deny all
