apiVersion: batch/v1
kind: Job
metadata:
  name: myjob
spec:
  completions: 5
  parallelism: 2
  template:
    spec:
      containers:
      - name: nginx
        image: nginx
      restartPolicy: Never
