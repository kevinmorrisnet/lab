# manager-api-svc.yaml  (broken — wrong selector intentionally)
apiVersion: v1
kind: Service
metadata:
  name: manager-api-svc
  namespace: mars
spec:
  type: ClusterIP
  ports:
  - port: 4444
    targetPort: 80
  selector:
    id: manager-api-deployment   # WRONG — points to deployment name not pod label
