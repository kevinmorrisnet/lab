apiVersion : apps/v1
kind: ReplicaSet
metadata:
  name: web-rs
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - image: nginx
        name: nginx
  selector:
    matchLabels:
      app: web

