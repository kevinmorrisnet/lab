apiVersion: v1
kind: Pod
metadata:
  name: cache-pod
spec:
  containers:
  - image: nginx
    name: nginx
    volumeMounts:
    - name: cache-vol
      mountPath: /cache
  volumes:
  - name: cache-vol
    emptyDir: {}

